package com.indieprog.jqw.entities;

public interface Entity {
	
	/**
	 * The only requirement in the Entity interface.  Each Entity must implement a draw() method.
	 */
	public void draw();
	
}
